{
  "homepage": "https://suzette-sousa.github.io/",
  "name": "suzette-sousa",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "export": "npm run build && next export",
    "deploy": "yarn export && touch out/.nojekyll && main -t -b gh-pages -d out"
  },
  "dependencies": {
    "next": "10.0.4",
    "react": "17.0.1",
    "react-dom": "17.0.1"
  },
  "devDependencies": {
    "frontmatter-markdown-loader": "^3.6.2",
    "gh-pages": "^3.1.0"
  }
}
